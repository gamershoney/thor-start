{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Thor",
            "type": "cppvsdbg",
            "request": "launch",

            "program": "${workspaceFolder}\\thor-start.exe",
            "cwd": "${workspaceFolder}",

            "preLaunchTask": "Build Thor Debug",

            "args": [],
            "stopAtEntry": false,
            "externalConsole": false
        }
    ]
}